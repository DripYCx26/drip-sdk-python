# Test dependencies for Drip SDK
# Install with: pip install -r tests/requirements.txt

# Core testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0
pytest-timeout>=2.0.0

# HTTP mocking
respx>=0.20.0

# Type stubs
types-flask>=1.1.0

# Install the SDK with all extras for testing
-e .[all,dev]
